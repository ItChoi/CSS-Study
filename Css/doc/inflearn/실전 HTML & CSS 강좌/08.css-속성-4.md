# 08 css-속성-4
	- float 속성
	- gradient 속성
	
	1) float 속성
		- position 속성과 함께 요소의 위치를 설정하기 위한 속성
		- 블록 속성 태그에 left 자리가 있으면 왼쪽으로 붙고 자리가 없으면 밑으로 떨어진다. 
		float 속성을 감싸고 있는 태그에 overflow:hidden;주기!
		-   
	
	2) gradient 속성
		- [gradient 사이트](http://colorzilla.com/gradient-editor/)
	